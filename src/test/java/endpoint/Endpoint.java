package endpoint;

public class Endpoint {
    public static final String
            userListEndpoint = "/users?page=2",
            userEndpoint = "/users/{id}",
            loginEndpoint = "/login";

}
